# Claude Notes

## Environment Setup
- The project uses a virtual environment (venv)
- Before running tests or any Python commands, use:
  ```bash
  .venv/bin/python -m pytest <test_file>
  ```
  or activate the venv first:
  ```bash
  source .venv/bin/activate
  ```
